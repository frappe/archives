# Main Configuration file

# List of all databases on the server with applications that are loaded in the
# database

databases = {
	'projects': {
		'applications': ['projects'],
	}
}

# Default Database

db_name = 'projects'
db_user = 'projects'
db_password = 'sample'

# Default language

language = 'en'

# App paths

# Path to app - by default all apps should be in the apps/ folder
# to override, mention here
# apps = { 'myapp', '/path/to/myapp' }